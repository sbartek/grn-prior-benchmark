# Top-level deps, pinned to the versions verified on Python 3.11 (Mac, uv, 2026-07-03).
# For an exact reproduction use requirements.lock.txt (full transitive freeze).
cellxgene-census==1.18.0
scanpy==1.11.5
anndata==0.12.19
decoupler==2.1.6
scikit-learn==1.9.0
torch==2.12.1
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
matplotlib==3.11.0
# optional stretch (GNN); install only if attempting the torch_geometric path
# torch-geometric
